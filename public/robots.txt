# Allow all crawlers full access. The site is a single-page app; there is
# nothing to disallow. Named per-bot groups are intentionally omitted: a
# named User-agent group OVERRIDES the wildcard for that bot rather than
# adding to it, so duplicating "Allow: /" per bot only creates a maintenance
# trap (a future Disallow under * would silently not apply to them).
User-agent: *
Allow: /

Sitemap: https://danielrltan.com/sitemap.xml
